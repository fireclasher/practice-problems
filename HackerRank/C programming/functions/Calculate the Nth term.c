#include<stdio.h>
#include<stdlib.h>
#include<math.h>
void find_nth_term(int a,int b,int c,int d){
    int cd;
    cd=abs(b-c);
    sum()
}
int main() {
    int n, a, b, c;
  
    scanf("%d %d %d %d", &n, &a, &b, &c);
    int ans = find_nth_term(n, a, b, c);
 
    printf("%d", ans); 
    return 0;
}