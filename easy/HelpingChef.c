#include <stdio.h>
void main()
{   int j,i,n;
    scanf("%d",&n);
    for(i=0;i<n;i++)
        scanf("%d",j);
        if ((j<10) && (j<20) && (j>-20))
            printf("Thanks for helping chef!");
        else{printf("-1");}
}